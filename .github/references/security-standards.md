# Security Standards

- no secrets in code
- no secrets in logs
- validate external input
- avoid unsafe deserialization
- avoid insecure temp file handling
- avoid risky reflection patterns
- mask sensitive identifiers and financial/personal data