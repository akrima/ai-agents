# Logging Standards

- never use `System.out.println`
- never use `System.err.println`
- use structured, meaningful logger messages
- avoid sensitive data in logs
- avoid noisy logs in hot paths
- use correct log levels